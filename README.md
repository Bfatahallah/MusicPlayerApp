# MusicPlayerApp
Music Player application using HTML, CSS, JavaScript, and React (with optional Tailwind CSS). The application will allow users to search for and play music tracks using a public music API. with the use of Deezer API to fetch and display music data.
